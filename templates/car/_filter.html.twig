{{ form_start(form, {attr: {class: 'filter'}}) }}
    {{ form_row(form.q) }}

    <h4>Prix</h4>
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.minPrice) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.maxPrice) }}
        </div>
    </div>
    <div class="mb-3" id="price-slider"></div>

    <h4>Année</h4>
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.minYear) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.maxYear) }}
        </div>
    </div>
    <div class="mb-3" id="year-slider"></div>

    <h4>Kilométrage</h4>
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.minMileage) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.maxMileage) }}
        </div>
    </div>
    <div class="mb-3" id="mileage-slider">Hello</div>

<button type="submit" class="btn btn-primary w-100">Appliquer le filtre</button>
{{ form_end(form) }}