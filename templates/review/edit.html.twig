{% extends 'base.html.twig' %}

{% block title %}Garage V. PARROT - Modification Avis{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center">
    <h1 class="my-5">Modifier l'avis {{review.title|title}} </h1>
    
</div>
<div class="container d-flex justify-content-center">
    <div class="col-3">
    <a href="{{ path('app_review_index') }}" class="btn btn-outline-secondary my-3">Retour à la liste</a>
        {{ form_errors(form) }}

        {{ form_start(form) }}

            {{ include('review/_form.html.twig', {'button_label': 'Modifier'}) }}
            {{ include('review/_delete_form.html.twig') }}
        
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}