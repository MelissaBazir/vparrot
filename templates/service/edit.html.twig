{% extends 'base.html.twig' %}

{% block title %}Garage V. PARROT - Modification Service{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center">
    <h1 class="my-5">Modifier le service {{service.title}} </h1>
    
</div>
<div class="container d-flex justify-content-center">
    <div class="col-3">
    <a href="{{ path('app_service_index') }}" class="btn btn-outline-secondary my-3">Retour à la liste</a>
        {{ form_errors(form) }}

        {{ form_start(form) }}

            {{ include('service/_form.html.twig', {'button_label': 'Modifier'}) }}
            {{ include('service/_delete_form.html.twig') }}
        
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}




{# 
{% block title %}Edit Service{% endblock %}

{% block body %}
    <h1>Edit Service</h1>

    {{ include('service/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_service_index') }}">back to list</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %} #}
