{% extends 'base.html.twig' %}

{% block title %}Garage V. PARROT - Nouvel Horaire{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center">
    <h1 class="my-5">Créer une annonce </h1>
</div>
<div class="container d-flex justify-content-center">
    <div class="col-8">
        {{ form_errors(form) }}

        {{ form_start(form) }}
                {{ include('opening/_form.html.twig', {'button_label': 'Créer l\'horaire'}) }}
                <a href="{{ path('app_opening_index') }}" class="btn btn-dark my-3">Retour à la liste</a>        {{ form_end(form) }}
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
